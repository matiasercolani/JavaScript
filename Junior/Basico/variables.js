//+ Variables - let y const funcionan dentro del scope donde fueron declarados y no fuera.

//* const puede ser modificado si es un objeto o un array

// Tipos de datos
cadena = 'cadena de texto o string';
numero = 10;
booleano = false;

// Declaraciones de variables var,let,const (var ya no se utiliza)
var global = 'alcance global';
let limitada = 'alcance regional';
const constante = 'no puede variar su contenido';

// Undefined = valor no declarado
let numero2;
//alert(numero2);


// Null = Valor nulo 
let nulo = null;


// NaN = Not a Number. Al hacer operaciones entre dos variables que no son numeros.
alert(cadena * numero);

// Otra forma de declarar variables
let uno = 1, dos = 2;
