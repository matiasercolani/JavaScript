// ! ------------------------------Objeto Math--------------------------------------------

let numero = 2;
let resultado;

// ! ------------------------------Metodos--------------------------------------------

// * sqrt() - Devuelve la raiz cuadrada positiva de un numero

// resultado = Math.sqrt(numero);

// * cbrt() - Devuelve la raiz cubica  de un numero

// resultado = Math.cbrt(numero);

// * max() / min() - Devuelve el mayor de cero o mas numeros.

// resultado = Math.max(4,5,2,12,67,8);

// * random() - Devuelve un numero aleatorio entre 0 y 1

// resultado = Math.random()*100;
// resultado = resultado.toString();
// let num = resultado[0] + resultado[1];
// if(resultado[1] == "."){
//     num = resultado[0];
// }


// * round() - Devuelve el valor de un numero redondeado al numero entero mas cercano

// entre 0 y 100 incluidos
// resultado = Math.random()*100;
// resultado = Math.round(resultado);

// * fround() - Devuelve la reprensentacion flotante de precision simple mas cercana de un numero. IRRELEVANTE

// resultado = Math.fround(9.55555555555555555555555555555555555555);

// * floor() - Devuelve el mayor entero menor que o igual a un numero

// entre 0 y 100 sin incluir
//  resultado = Math.random()*99;
//  resultado = Math.floor(resultado + 1);

// * trunc() - Devuelve la parte entera del numero x, la eliminacion de los digitos fraccionarios

// resultado = Math.trunc(9.9)

// ! ------------------------------Propiedades--------------------------------------------


// * PI - Ratio de la circunferencia de un circulo respecto a su diametro, aproximadamente 3.14159

// resultado = Math.PI;

// * SQRT1_2 - Raiz cuadrada de 1/2; Equivalentemente, 1 sobre la raiz cuadrada de 2, aproximadamente

// resultado = Math.SQRT1_2;

// * SQRT2 -  Raiz cuadrada de 2, aproximadamente 1.414

// resultado = Math.SQRT2;


// ! ---------------------------------No imortantes-----------------------------------------

// * E -  Constante de Euler, la base de los logaritmos naturales, aproximadamente

// resultado = Math.E;

// * LN2 -  Logaritmo natural de 2, aproximadamente 0.693

// resultado = Math.LN2;

// * LN10 -  Logaritmo natural de 10, aproximadamente 2.303

// resultado = Math.LN10;

// * LOG2E -  Logaritmo natural de 2, aproximadamente 1.443

// resultado = Math.LOG2E;

// * LOG10E -  Logaritmo de E con base 10, aproximadamente 0.434

// resultado = Math.LOG10E;


document.write("Resultado: <b>" + resultado + "</b><br>");
//+ document.write(num);



